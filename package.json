{
  "name": "wirralbears-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start --workspace=backend\" \"npm run start --workspace=frontend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "keywords": [],
  "author": "jadd0",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "env": "^0.0.2"
  }
}
