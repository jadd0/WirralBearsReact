{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build:packages && pnpm --filter web run build",
  "outputDirectory": "apps/web/.next"
}
