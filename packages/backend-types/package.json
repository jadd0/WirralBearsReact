{
  "name": "@wirralbears/backend-types",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@wirralbears/backend": "workspace:../../apps/backend"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "prebuild": "pnpm run clean"
  }
}