{
  "name": "@wirralbears/validation",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "dependencies": {
    "@flayva/constants": "workspace:*",
    "zod": "3.24.2",
    "zod-form-data": "^2.0.7"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "prebuild": "pnpm run clean"
  }
}
