{
  "name": "@wirralbears/constants",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "type": "module",
  "dependencies": {},
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "prebuild": "pnpm run clean"
  }
}